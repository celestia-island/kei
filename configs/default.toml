# Default board configuration template

[board]
name = ""
soc = ""
arch = "aarch64"

[kernel]
# One-time vendored baseline, NOT a tracked upstream version:
# kei no longer absorbs upstream Asterinas.
asterinas_baseline = "0.18.0"
bsp_crate = ""
dtb = ""
features = []

[qemu]
machine = "virt"
cpu = "cortex-a55"
memory = "2048"
smp = 4

[output]
kernel_image = "kei-kernel.bin"
dtb_image = ""
