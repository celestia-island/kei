[package]
edition = "2021"
name = "kei-qemu-mps2"
version = "0.1.0"
publish = false
# Member of the library's own workspace, rooted at the parent directory.
# This crate lives INSIDE `packages/kei/` so that copying that one directory
# yields a complete, loadable workspace — the demo used to sit outside it
# (`packages/qemu-mps2`) and referenced the library through `../kei`, which
# made the package impossible to extract on its own.
workspace = ".."

[dependencies]

# The kei wire protocol library — the parent directory of this crate.
kei = { path = "..", default-features = false, features = [
    "wire",
    "manifest",
    "hal",
] }

# Heap allocator for `alloc` (kei's wire protocol uses Vec<u8> for frames).

linked_list_allocator = "0.10"
[target.'cfg(target_os = "none")'.dependencies]
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7"
panic-halt = "0.2"
critical-section = "1"

[profile.release]
debug = 2
opt-level = 3
lto = false
panic = "abort"
